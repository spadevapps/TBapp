<h1>I am The departments</h1>
<div class="header">
  <div class="container">
  </div>
</div>

<div class="tags">
  <div class="container">
    <div class="cards row">
    	<% @departments.each do |t| %>
      <div class="card col-xs-4">
        <h2><%= t.name %></h2>
        <%= link_to "View Books", department_path(t) %>
      </div>
      <% end %>
    </div>
  </div>
</div>
